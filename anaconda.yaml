anaconda_config_version: 1
upstream_sources:
  - gitlab:
      identifier: gl2ps/gl2ps
      host: gitlab.onelab.info
      use_max_tag: true
      include_regex: ^gl2ps_\d+_\d+_\d+$
      from_pattern: ^gl2ps_(\d+)_(\d+)_(\d+)$
      to_pattern: \1.\2.\3
